version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries/"
    schema: "./db/schema/schema.sql"
    gen:
      go:
        package: "repository"
        sql_package: "pgx/v5"
        out: "./internal/repository"
        emit_json_tags: true